using System;
using TMPro;
using Unity.VisualScripting;
using UnityEngine;

public class ShootingScript : MonoBehaviour
{
   public GameObject Cross;
   

    // Update is called once per frame
    void Update()
    {
     
    }

    public void Shoot()
    {
        // Cross.SetActive(true); 
         //Playermovement.Sharp = true;
    }
}
